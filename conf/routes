# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)


# Profile routes

POST    /api/profiles            controllers.ProfileController.createProfile()
GET     /api/profiles            controllers.ProfileController.getAllProfiles()
GET     /api/profiles/:id        controllers.ProfileController.getProfile(id: Long)
PUT     /api/profiles/:id        controllers.ProfileController.updateProfile(id: Long)
DELETE  /api/profiles/:id        controllers.ProfileController.deleteProfile(id: Long)


GET   /@evolutions              controllers.ApplicationEvolutions.showEvolutions
