# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)


# Profile routes

POST    /api/profiles            controllers.ProfileController.createProfile()
GET     /profiles            controllers.ProfileController.getAllProfiles()
GET     /profiles/:id        controllers.ProfileController.getProfile(id: Long)
PUT     /profiles/:id        controllers.ProfileController.updateProfile(id: Long)
DELETE  /profiles/:id        controllers.ProfileController.deleteProfile(id: Long)


GET   /@evolutions              controllers.ApplicationEvolutions.showEvolutions
